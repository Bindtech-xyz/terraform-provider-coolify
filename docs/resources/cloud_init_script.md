---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_cloud_init_script Resource - coolify"
subcategory: "Cloud Provisioning"
description: |-
  A reusable cloud-init YAML document applied to VMs provisioned by coolify_cloud_server. The script must be valid cloud-init YAML (starting with #cloud-config).
---

# coolify_cloud_init_script (Resource)

A reusable cloud-init YAML document applied to VMs provisioned by `coolify_cloud_server`. The script must be valid cloud-init YAML (starting with `#cloud-config`).

## Example Usage

```terraform
resource "coolify_cloud_init_script" "base" {
  name   = "base-hardening"
  script = <<-EOT
    #cloud-config
    package_update: true
    packages: [fail2ban]
  EOT
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name.
- `script` (String) cloud-init YAML content.

### Read-Only

- `uuid` (String) Server-assigned identifier.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_cloud_init_script.example <uuid>
```
