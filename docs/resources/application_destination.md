---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_application_destination Resource - coolify"
subcategory: "Workloads"
description: |-
  Attaches an additional standalone Docker destination to an application (multi-destination deployment) — not for the primary destination, which is destination_uuid on coolify_application itself. Coolify rejects a destination already on the same server as the primary or another attached destination. Requires Coolify >= v4.2.0.
---

# coolify_application_destination (Resource)

Attaches an *additional* standalone Docker destination to an application (multi-destination deployment) — not for the primary destination, which is `destination_uuid` on `coolify_application` itself. Coolify rejects a destination already on the same server as the primary or another attached destination. Requires Coolify >= v4.2.0.

## Example Usage

```terraform
# Attach a second standalone Docker destination to an application, on a
# different server than its primary destination.
resource "coolify_application_destination" "web_secondary" {
  application_uuid = coolify_application.web.uuid
  destination_uuid = data.coolify_destinations.secondary.destinations[0].uuid
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `application_uuid` (String) UUID of the application. Changing it forces replacement.
- `destination_uuid` (String) UUID of the additional destination to attach. Changing it forces replacement.
