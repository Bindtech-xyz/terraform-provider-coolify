---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_environment Resource - coolify"
subcategory: "Organization"
description: |-
  An environment inside a Coolify project (e.g. production, staging). Applications, databases and services are always placed in an environment. Note that Coolify creates a default production environment with every project.
---

# coolify_environment (Resource)

An environment inside a Coolify project (e.g. `production`, `staging`). Applications, databases and services are always placed in an environment. Note that Coolify creates a default `production` environment with every project.

## Example Usage

```terraform
resource "coolify_environment" "staging" {
  project_uuid = coolify_project.example.uuid
  name         = "staging"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the environment, unique within the project.
- `project_uuid` (String) UUID of the parent project. Changing it forces replacement.

### Optional

- `description` (String) Free-form description.

### Read-Only

- `uuid` (String) Server-assigned identifier.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_environment.staging <project-uuid>/staging
```
