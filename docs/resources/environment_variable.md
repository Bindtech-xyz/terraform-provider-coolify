---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "coolify_environment_variable Resource - coolify"
subcategory: "Configuration & Backups"
description: |-
  An environment variable attached to an application, a service or a database. For team/project/environment/server-scoped variables, use coolify_shared_environment_variable instead.
---

# coolify_environment_variable (Resource)

An environment variable attached to an application, a service or a database. For team/project/environment/server-scoped variables, use `coolify_shared_environment_variable` instead.

## Example Usage

```terraform
resource "coolify_environment_variable" "database_url" {
  parent_type = "application"
  parent_uuid = coolify_application.web.uuid
  key         = "DATABASE_URL"
  value       = coolify_database.pg.internal_db_url
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String) Variable name. Changing it forces replacement (the API identifies variables by key).
- `parent_type` (String) Kind of resource the variable belongs to: `application`, `service` or `database`. Changing it forces replacement.
- `parent_uuid` (String) UUID of the parent resource. Changing it forces replacement.

### Optional

- `comment` (String) Free-form comment (max 256 characters).
- `is_buildtime` (Boolean) Available at build time. Defaults to `true`.
- `is_literal` (Boolean) Treat `$` sequences literally instead of interpolating. Defaults to `false`.
- `is_multiline` (Boolean) Value spans multiple lines. Defaults to `false`.
- `is_preview` (Boolean) Also inject into preview deployments. Defaults to `false`.
- `is_runtime` (Boolean) Available at runtime. Defaults to `true`.
- `value` (String, Sensitive) Variable value.

### Read-Only

- `uuid` (String) Server-assigned identifier.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import coolify_environment_variable.database_url application/<app-uuid>/DATABASE_URL
```
