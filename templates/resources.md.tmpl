---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: "{{- if or (eq .Name "coolify_server") (eq .Name "coolify_private_key") (eq .Name "coolify_destination") (eq .Name "coolify_server_settings") -}}
  Infrastructure
{{- else if or (eq .Name "coolify_project") (eq .Name "coolify_environment") (eq .Name "coolify_tag") (eq .Name "coolify_resource_tag") -}}
  Organization
{{- else if or (eq .Name "coolify_application") (eq .Name "coolify_database") (eq .Name "coolify_service") (eq .Name "coolify_application_destination") (eq .Name "coolify_application_preview") -}}
  Workloads
{{- else if or (eq .Name "coolify_environment_variable") (eq .Name "coolify_environment_variables") (eq .Name "coolify_shared_environment_variable") (eq .Name "coolify_storage") (eq .Name "coolify_volume_backup") (eq .Name "coolify_database_backup") (eq .Name "coolify_s3_storage") (eq .Name "coolify_scheduled_task") -}}
  Configuration & Backups
{{- else if eq .Name "coolify_notification_settings" -}}
  Notifications
{{- else if or (eq .Name "coolify_github_app") (eq .Name "coolify_gitlab_app") -}}
  CI/CD Sources
{{- else if or (eq .Name "coolify_cloud_token") (eq .Name "coolify_cloud_init_script") (eq .Name "coolify_cloud_server") -}}
  Cloud Provisioning
{{- else if or (eq .Name "coolify_resource_action") (eq .Name "coolify_deployment") -}}
  Operations
{{- else if eq .Name "coolify_api_settings" -}}
  Instance
{{- end}}"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExamples -}}
## Example Usage

{{- range .ExampleFiles }}

{{ tffile . }}
{{- end }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}
{{- if or .HasImport .HasImportIDConfig .HasImportIdentityConfig }}

## Import

Import is supported using the following syntax:
{{- end }}
{{- if .HasImportIdentityConfig }}

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

{{tffile .ImportIdentityConfigFile }}

{{ .IdentitySchemaMarkdown | trimspace }}
{{- end }}
{{- if .HasImportIDConfig }}

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

{{tffile .ImportIDConfigFile }}
{{- end }}
{{- if .HasImport }}

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}
